name: Playwright Tests

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: lts/*

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      - name: Run Playwright tests
        id: playwright
        continue-on-error: true
        run: npx playwright test

      - name: Upload Playwright Report
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30

      - name: Send Email Notification
        if: ${{ !cancelled() }}
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          secure: true

          username: ${{ secrets.SMTP_USERNAME }}
          password: ${{ secrets.SMTP_PASSWORD }}

          subject: "Playwright Test Result - ${{ steps.playwright.outcome }}"

          to: |
            ${{ secrets.EMAIL_TO_1 }}
            ${{ secrets.EMAIL_TO_2 }}

          from: ${{ secrets.SMTP_USERNAME }}

          body: |
            Hello,

            Playwright automation execution has completed.

            Repository: ${{ github.repository }}
            Branch: ${{ github.ref_name }}

            Test Status: ${{ steps.playwright.outcome }}

            Please check the GitHub Actions run for the detailed
            Playwright test report.

            Regards,
            QA Automation

      - name: Mark workflow as failed if tests failed
        if: ${{ steps.playwright.outcome == 'failure' }}
        run: exit 1